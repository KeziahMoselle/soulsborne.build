{
  "name": "soulsborne.build",
  "version": "0.1.0",
  "description": "share your soulsborne builds",
  "author": "Keziah MOSELLE <contact@keziahmoselle.fr>",
  "scripts": {
    "setup": "cd backend && yarn install && cd ../frontend && pnpm install",
    "dev": "concurrently \"npm:dev:payload\" \"npm:dev:astro\"",
    "dev:payload": "cd backend && yarn dev",
    "dev:astro": "cd frontend && pnpm dev"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
